package com.example.ottokeng.domain.find.repository;

import com.example.ottokeng.domain.find.entity.FindWriting;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FindWritingRepository extends JpaRepository<FindWriting, Long> {
}